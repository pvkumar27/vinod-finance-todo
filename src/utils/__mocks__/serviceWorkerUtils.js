/**
 * Mock utility functions for service worker registration
 */

/**
 * Mock register the Firebase messaging service worker
 * @returns {Promise<null>}
 */
export const registerMessagingServiceWorker = async () => {
  console.log('Mock: Firebase messaging service worker registered');
  return null;
};
