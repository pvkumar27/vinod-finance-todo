import React from 'react';
import TaskManager from './TaskManager';

// Simplified version that just renders the TaskManager component
const ToDoTest = () => {
  return <TaskManager />;
};

export default ToDoTest;
