import React from 'react';
import ExpensesDashboard from './ExpensesDashboard';

// Simplified version that just renders the dashboard component
const ExpensesTest = () => {
  return <ExpensesDashboard />;
};

export default ExpensesTest;